# ДЗ
Ширяков_ДР_ИКБО-63-24 выполнение домашнего задания по конфигурационному управлению
1. Была создана программа, согласно условиям 30 варианта которая можнет принимать: входной текст, выполнять анализ синтаксиса, выводить результаты вычислений и в случае ошибок уведомлять пользователя программы.
2. Числа задаются в восьмеричном формате, так же создаются массивы для содержания вложенных нами значений, именна констант состоят из латинских букв [a-z]+
Объявление констант происходит через функцию define, для вычислений на этапе трансляции используется следующая конструкция: ^(выражение)
Поддерживаемые операции и функции:
2.1 + - сложение
2.2 - вычитание
2.3 * - умножение
2.4 max() - максимум
2.5 pow() - возведение в степень
3. Основная команда для использования программой выполнялась через PowerShell: "C:\Users\User\AppData\Local\Programs\Python\Python314\python.exe" "C:\Users\User\Desktop\1\Konf.py" "(define a 0o10); (define b 0o4); (define sum ^(a+b)); (define diff ^(a-b)); (define prod ^(a*b)); (define maximum ^(max(a,b))); (define power ^(pow(a,b)));"
4. Пример использования: Информационные технологи, расчёт ядер, потоков и значений памяти байт у процессора. С помощью ввода следующей команды в терминале: python "C:\Users\User\Desktop\1\Konf.py" '(define cores 0o10); (define threads ^(cores * 0o2)); (define memory [0o400, 0o1000, 0o2000]);' получили необходимые нам значения. Может пригодиться для подсчёта серверных мощностей.
